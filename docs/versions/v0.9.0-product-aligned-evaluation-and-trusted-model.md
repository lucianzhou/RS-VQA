# v0.9.0 产品对齐评测与可信研究模型体验

状态：已完成

日期：2026-07-27

论文题目固定为《跨模态特征融合机制及微调策略研究与应用》。

## 本版目标

本版不训练模型，也不宣称 checkpoint 改善。目标是把研究侧固定的 predicted-soft 模型、
产品对齐评测和能力证据接入独立应用，证明工程推理链不改变模型行为，并把真实短板透明展示给用户。

主流程保持：

```text
上传图像 -> 自然中文或英文提问 -> 固定发布推理 -> 可信答案展示
         -> 可选 RS-Bot 解释/检索/汇总 -> 可追溯历史与报告
```

## 已实现

- 模型、评测 release schema、固定 ID 与哈希契约。
- Real Runtime 对预期 release ID 和 manifest SHA-256 的启动固定；缺失时 fail closed。
- checkpoint、55 类词表、runtime wheel、预处理器树的逐制品校验。
- 8/512/3072 三集合隔离、路径越界防护、只读与 inventory 校验。
- diagnostic-test 显式 sealed gate 和不持久化逐样本预测的聚合 runner。
- corrected 55 类 OA/AA、Count、yes/no 和 Area 规范化评分。
- 原始问题、canonical question、实际模型输入问题和 normalizer 版本的持久化。
- 研究答案、Mock、Gemini、Qwen3 和 RS-Bot 的独立来源标识。
- confidence、margin、top-k、题型概率、哈希、review policy 和能力边界的可展开 UI。
- Count 非零/密集低估提示；默认不启用自动拒答。
- RAG 失败索引可在原文档 ID 上幂等恢复，BGE/Milvus 引用链已验证。
- 并行项目刷新不会把刚创建并正在工作的活动会话错误切走。

## 当前验证证据

- 模型服务：82 项测试通过。
- Gateway：90 项通过、2 项按配置跳过；MCP 协议与 PostgreSQL/Flyway/Redis 2 项显式集成测试通过。
- Web：43 项测试、类型检查和生产构建通过。
- Playwright：`--workers=1 --retries=0` 下桌面、笔记本、平板和手机 16/16 通过。
- RAG：3 条基准 `Recall@5=1.0`、`MRR=1.0`。
- 新 release `rsvqa-hr-qdrop15-predicted-soft-20260727-9b4ade2` 已通过真实 CPU `/ready`、
  Presence/Count/Area/Comparison 中英文 parity、HTTP 服务/直接 release runtime 一致性。
- 模型 manifest、checkpoint、runtime wheel SHA-256 分别为
  `cce9b8bb48d5cf0213ce789290ceea7525ad2c1d96eba66867c733f1bbc78045`、
  `2426770af96a6f41b30e081c9719d6582471fab091e4b44ba2c3068d6e227109`、
  `161f00c81f72d54c145b016fbc5c1cb8ed9f0822c3b17682f67b536330887734`。
- 评测 release `rsvqa-hr-product-aligned-eval-20260727-1796e90` manifest SHA-256 为
  `aa35fa18f442e6fc9f4f648034a7b0ad019c31aa20219de8118780c4d5cbc5c4`。
- golden-replay `8/8`；provider-dev `436/512`，OA/AA=`0.8515625/0.8515625`；
  sealed diagnostic-test `2535/3072`，OA/AA=`0.8251953125/0.8251953125`，
  top-5=`2974/3072`。未持久化逐样本预测。
- Real + RAG 冷启动后九个容器全部健康；研究模型、Qwen3-VL 与 RS-Bot 确定性工具链
  完成真实 smoke。Gemini 中转站在验收时把已列出的模型错误路由到不存在的内部模型并返回
  HTTP 500，因此系统将 Gemini 和 Gemini 规划保持 fail-closed，而不是显示为可用。
- 首轮 Playwright 暴露“新建分析后立即上传”会把图像写入旧会话的竞态；修复并新增单元测试后，
  最终无重试串行 E2E 为 16/16。

## 研究事实与能力边界

- 正式 full test OA/AA：`0.8401412/0.8390032`。
- 正式 full test_phili OA/AA：`0.8031558/0.7975786`。
- 不声称 SOTA；predicted-soft 相对 none 的配对置信区间包含 0。
- 模型是 RSVQA-HR grouped 55-answer closed-set classifier，不是开放式视觉助手。
- diagnostic 显示非零/密集 Count、长尾答案和 Philadelphia 地域迁移仍是模型短板。
- confidence/margin 不保证答案正确，全局自动拒答默认关闭。

## 完成情况

1. 模型与评测发布已受控取得，传输分片校验后删除，AutoDL 无卡实例已关机。
2. 新 release CPU `/ready`、真实 smoke、canonical parity 和 golden 8/8 通过。
3. provider-dev 与 sealed diagnostic 只按冻结规则生成聚合结果。
4. Qwen3、RS-Bot、RAG/MCP 与真实研究模型通过；Gemini 外部中转故障被正确隔离。
5. 完整 Real + RAG Compose 冷启动、串行响应式 E2E 和禁止制品审计完成。
6. 本版没有训练、没有改变 checkpoint，也没有解决非零密集 Count、长尾答案或跨地域迁移短板。

## Gemini 恢复复验

2026-07-27 中转站恢复且用户轮换已暴露的 Key 后，重新完成：

- OpenAI 兼容文本、SSE、结构化输出、tool calling 和 image input 五项 live smoke。
- 应用内 Gemini-3.6-flash 真实图像问答，来源保持 `EXTERNAL_VLM`。
- RS-Bot `LLM_PLANNING` 三步规划，依次调用 `current_model_release` 与
  `model_capabilities`，provider model 为 `gemini-3.6-flash`。

Gemini、Qwen3-VL 和研究模型均显示 `CONFIGURED`。这只说明接口与工具链恢复，不构成
外部模型质量、稳定性或持续可用性保证；中转异常时仍按 fail-closed 处理。
