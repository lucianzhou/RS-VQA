server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: rs-vqa-gateway
  datasource:
    url: ${RSVQA_DATABASE_URL:jdbc:postgresql://localhost:5432/rsvqa}
    username: ${RSVQA_DATABASE_USER:rsvqa}
    password: ${RSVQA_DATABASE_PASSWORD:rsvqa_dev_only}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
  data:
    redis:
      host: ${RSVQA_REDIS_HOST:localhost}
      port: ${RSVQA_REDIS_PORT:6379}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 128MB
  lifecycle:
    timeout-per-shutdown-phase: 20s
  ai:
    mcp:
      client:
        enabled: ${RSVQA_MCP_CLIENT_ENABLED:false}
        name: rs-vqa-mcp-client
        version: 0.3.0
        request-timeout: 10s
        type: SYNC
        streamable-http:
          connections:
            readonly-tools:
              url: ${RSVQA_MCP_REMOTE_URL:http://localhost:18080}
              endpoint: ${RSVQA_MCP_REMOTE_ENDPOINT:/mcp}
      server:
        enabled: ${RSVQA_MCP_ENABLED:true}
        name: rs-vqa-readonly-tools
        version: 0.3.0
        protocol: STATELESS
        type: SYNC
        annotation-scanner:
          enabled: true

rsvqa:
  gemini:
    enabled: ${RSVQA_GEMINI_ENABLED:false}
    api-key: ${GEMINI_API_KEY:}
    model: ${RSVQA_GEMINI_MODEL:gemini-2.5-flash}
    timeout-seconds: ${RSVQA_GEMINI_TIMEOUT_SECONDS:60}
    max-retries: ${RSVQA_GEMINI_MAX_RETRIES:2}
    max-output-tokens: ${RSVQA_GEMINI_MAX_OUTPUT_TOKENS:1024}
    temperature: ${RSVQA_GEMINI_TEMPERATURE:0.2}
  qwen:
    enabled: ${RSVQA_QWEN_ENABLED:false}
    api-key: ${DASHSCOPE_API_KEY:}
    model: ${RSVQA_QWEN_MODEL:qwen3-vl-32b-instruct}
    timeout-seconds: ${RSVQA_QWEN_TIMEOUT_SECONDS:60}
    max-retries: ${RSVQA_QWEN_MAX_RETRIES:2}
    max-output-tokens: ${RSVQA_QWEN_MAX_OUTPUT_TOKENS:1024}
    temperature: ${RSVQA_QWEN_TEMPERATURE:0.2}
  model-service:
    base-url: ${RSVQA_MODEL_SERVICE_URL:http://localhost:8000}
    timeout-seconds: 15
    max-file-bytes: 10485760
  storage:
    root: ${RSVQA_STORAGE_ROOT:./runtime/uploads}
  knowledge-service:
    base-url: ${RSVQA_KNOWLEDGE_SERVICE_URL:http://localhost:8010}
    timeout-seconds: 90
    default-index-version: ${RSVQA_KNOWLEDGE_INDEX_VERSION:rsvqa-knowledge-v1}
  demo-auth:
    enabled: ${RSVQA_DEMO_AUTH:true}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
