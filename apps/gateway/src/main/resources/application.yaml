server:
  port: 8080

spring:
  application:
    name: rs-vqa-gateway
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 11MB

rsvqa:
  model-service:
    base-url: http://localhost:8000
    timeout-seconds: 15
    max-file-bytes: 10485760
