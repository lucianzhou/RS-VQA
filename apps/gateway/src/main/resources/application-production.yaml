server:
  servlet:
    session:
      cookie:
        secure: true
        http-only: true
        same-site: lax

rsvqa:
  demo-auth:
    enabled: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health
